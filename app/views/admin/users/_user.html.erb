<li>
  <%= gravatar_for user,size: 50 %>
  <%= link_to user.username, [:admin, user] %>
  <% if user_signed_in? && current_user.admin? && !current_user?(user) %>
  | <%= link_to t("links.delete"), [:admin, user], method: :delete, data: {confirm: "You sure"} %>
  <% end %>
</li>
